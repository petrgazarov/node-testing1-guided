function foo() {
  return 5
}

// Test away!
describe('our first tests', () => {

  it('works', () => {

    expect(foo).toBeDefined() // assertion
    expect(foo).
  })

  test('works some more', () => {

  })

})
